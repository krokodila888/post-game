/*function oddOrNot(str) {
  let arr;
  arr = str.split(' ');
  let one = arr[0] % 2;
  let two = arr[1] % 2;
  function pass(num) {
    return 
  };
  if (one !== two) {
    let three = arr[2] % 2;
    if (one === three) return two; else return one
  }
  else if (one === two) {
    let aaa;
    for (let i = 2; i < (arr.length); i++) {
      if (arr[i] % 2 !== one) {
        break;
      }
    };
    return aaa
  }

  

  for (let i = 1; i < (arr.length + 1); i++) {
    //если второй номер неравен первому, смотрим третий; если третий снова неравен, возвращаем 2, если равен, возвращаем 1
    //если втрой номер равен первому, листаем дальше и возвращаем первый же, который будет неравен 

    /*
    for (i=0;i<=10;i++)
  {
  if (i==3)
    {
    break;
    }
  document.write("Число i равно " + i);
  document.write("<br />");
  }
  */
  /*  if (odd = 1 && arr[i] % 2 === res) {
      odd = 1
    }



    if (change === false && (arr[i] % 2) !== res) {
      count = i;
      change = true 
    }
    else if (change === true && (arr[i] % 2) !== res) {

    }
  };
  return (count - 1)
};

console.kog(oddOrNot("2 4 7 8 10"))



while (i < 6) {
  if (i === 3) {
    break;
  }
  i = i + 1;
}*/